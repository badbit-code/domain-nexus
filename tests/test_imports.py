from .mail import send

"""try:
    from . import only_import
except Exception as e:
    print(e)
    send(str(e))
    send(str(e), "ganesh-kumar@live.com")
else:
    send("Completed")
    send("Completed", "ganesh-kumar@live.com")"""
